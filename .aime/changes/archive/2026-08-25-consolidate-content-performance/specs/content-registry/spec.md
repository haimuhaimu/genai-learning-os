## ADDED Requirements

### Requirement: 统一内容清单模型
系统 SHALL 提供一个前端类型安全的内容 registry 作为管理层。每个条目 MUST 至少包含全局唯一 `id`、内容 `type`、标题、合法入口（page 与可选参数）、受控标签、层级归属、发布状态，并 SHALL 能声明关联实验和关联案例；registry SHALL 通过适配器引用现有数据，不要求所有正文采用同一 schema。

#### Scenario: 注册不同内容类型
- **WHEN** 课程、案例、实验、视频或论文被加入统一清单
- **THEN** 每个对象都可通过统一字段被识别、分组、筛选和导航
- **AND** 对象的正文仍可保留在既有数据文件和组件 schema 中

#### Scenario: 表达内容关联
- **WHEN** 一个课程或资源配置了关联实验或案例
- **THEN** registry 使用目标对象的唯一 ID 保存关联
- **AND** 消费方可以解析出目标对象的合法入口而不复制路由常量

### Requirement: 受控词表与状态
系统 SHALL 为内容类型、标签、层级和发布状态提供类型或常量定义；面向聚合页的默认查询 MUST 只返回当前可见状态的条目，非公开状态的处理规则 MUST 明确且可测试。

#### Scenario: 默认隐藏非公开内容
- **WHEN** registry 同时包含公开和非公开状态条目
- **THEN** 面向用户的聚合页默认只展示被定义为可见的条目
- **AND** 非公开条目不会因任意字符串筛选而泄露到列表

#### Scenario: 使用受控标签筛选
- **WHEN** 用户选择一个 registry 声明的受控标签
- **THEN** 聚合结果只包含声明该标签且状态可见的条目
- **AND** 筛选标签名称与内容卡片显示一致

### Requirement: Registry 完整性校验
系统 MUST 在测试期校验 registry，并 SHALL 在开发运行时执行同一组核心校验：全局 ID 唯一、page 属于合法路由、路由参数名合法、标签属于受控集合、层级引用合法、关联实验与案例对象存在且类型匹配。校验结果 MUST 包含可定位到条目和字段的错误信息。

#### Scenario: 检测重复 ID
- **WHEN** 两个任意类型的 registry 条目使用相同 ID
- **THEN** 校验失败并报告重复 ID 及冲突条目

#### Scenario: 检测非法路由
- **WHEN** 条目的 page 不在路由清单中或入口包含未声明的参数名
- **THEN** 校验失败并报告该条目的入口字段

#### Scenario: 检测非法标签
- **WHEN** 条目声明不在受控词表中的标签
- **THEN** 校验失败并报告条目 ID 与非法标签

#### Scenario: 检测悬空或错型关联
- **WHEN** 条目关联不存在的对象，或关联实验字段指向非实验对象，或关联案例字段指向非案例对象
- **THEN** 校验失败并报告来源 ID、关系类型与目标 ID

#### Scenario: 合法清单通过校验
- **WHEN** 所有条目满足唯一性、路由、词表、层级和关联约束
- **THEN** 测试期与开发运行时校验均返回成功

### Requirement: Registry 驱动三个聚合页
学堂、实验室和工具箱聚合页 SHALL 从统一 registry 查询条目，不得分别维护会漂移的平行入口数组；学堂 SHALL 聚合课程与学习路线，实验室 SHALL 聚合所有实验，工具箱 SHALL 在同一资源区聚合视频与论文。

#### Scenario: 学堂按层级与标签展示内容
- **WHEN** 用户进入学堂并选择层级或标签
- **THEN** 列表由 registry 查询得到且只展示同时满足条件的可见课程或路线
- **AND** 每个结果导航到 registry 声明的入口

#### Scenario: 实验室筛选全部实验
- **WHEN** 用户在实验室按标签、层级或关联案例筛选
- **THEN** 结果覆盖 registry 中来自各既有实验体系的匹配实验
- **AND** 不要求用户先知道 foundation、expert、agent、paper 或 distill 的旧分组入口

#### Scenario: 工具箱切换资源类型
- **WHEN** 用户在工具箱选择全部、视频或论文类型
- **THEN** 列表从同一 registry 查询相应资源
- **AND** 视频和论文卡片保留各自必要的现有信息与打开行为

### Requirement: 聚合筛选与状态可访问
聚合页的筛选控件 MUST 可通过键盘操作并具有关联标签；结果数量或状态变化 SHALL 以可感知方式反馈。无匹配内容时 SHALL 显示清晰空状态并提供清除筛选或返回全部内容的操作。

#### Scenario: 筛选得到空结果
- **WHEN** 当前类型、标签、层级和状态组合没有匹配内容
- **THEN** 页面显示“没有匹配内容”类明确说明而不是空白区域
- **AND** 提供键盘可用的清除筛选操作

#### Scenario: 键盘执行筛选
- **WHEN** 键盘用户聚焦筛选控件并改变选项
- **THEN** 列表按相同查询规则更新
- **AND** 用户可感知更新后的结果数量或空状态

### Requirement: 校验失败的用户兜底
生产渲染遇到 registry 不可用、条目不完整或关系解析失败时 MUST 隔离无效条目，并 SHALL 在受影响聚合页展示可理解的降级说明与可用恢复动作；单个无效条目 MUST NOT 导致整个应用白屏。

#### Scenario: 单个条目解析失败
- **WHEN** 聚合页读取到一个无法解析的条目
- **THEN** 系统跳过或标记该条目并继续展示其他合法内容
- **AND** 页面提供刷新、清除筛选或返回首页中的至少一个恢复动作

#### Scenario: Registry 整体不可用
- **WHEN** 聚合页无法获得任何有效 registry 数据
- **THEN** 主内容区域显示带标题、原因说明和恢复动作的错误兜底
- **AND** 全局导航及旧直达 URL 仍可使用
