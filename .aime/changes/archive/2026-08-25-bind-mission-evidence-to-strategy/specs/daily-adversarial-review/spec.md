## ADDED Requirements

### Requirement: 每日角色轮换与审查记录
系统 SHALL 使用受控角色序列执行每日对抗式审查，至少包含“算法怀疑者”“学习迁移审计者”“可靠性破坏者”“维护成本审计者”和“可访问性反方”；每次审查 MUST 记录日期、角色 key、角色关注点、覆盖范围和是否由用户显式指定。用户指定角色时 MUST 覆盖轮换结果但不得改写后续轮换顺序；未指定时 SHALL 从最近一次已记录角色的下一项继续循环。

#### Scenario: 用户固定今日角色
- **WHEN** 用户指定“算法怀疑者”执行今日审查
- **THEN** 审查记录使用该角色及其证据标准
- **AND** 下一次自动轮换仍从原序列应到位置继续，不因本次覆盖永久跳位

#### Scenario: 自动轮换角色
- **WHEN** 新一日审查没有指定角色且存在上一条有效审查记录
- **THEN** harness 选择受控序列中的下一角色
- **AND** 输出本次角色、关注点和与上一角色不同的主要攻击面

### Requirement: 实证问题准入门槛
每个进入候选清单的问题 MUST 同时具备可定位实现或规范、可复现观察、被违反的产品承诺、用户或证据影响、现有测试未能阻止该行为的说明，以及一天内可完成的最小修复边界。仅有审美偏好、假设性担忧、不可复现推测或新增功能愿望的问题 MUST NOT 进入候选清单。

#### Scenario: 可复现的假完成问题进入候选
- **WHEN** 审查者能用固定输入证明旧压力结果与新策略被合并为完成证据，并定位状态推导、存储合并和测试缺口
- **THEN** 问题满足实证准入门槛
- **AND** 候选记录包含复现命令或操作步骤、实际结果、期望结果和代码位置

#### Scenario: 只有看似合理的担忧
- **WHEN** 审查者无法给出确定输入、可观察结果或被违反的现有承诺
- **THEN** harness 将该项记为待调查假设而不是实证问题
- **AND** 该项不得用于创建今日修复 change

### Requirement: 高价值单切片选择
每日审查 SHALL 输出 1-3 个满足准入门槛的候选问题，并 MUST 只选择一个当天可闭环的实施切片。选择 SHALL 按“错误完成或安全误导优先、学习判断影响、影响面、可复现性、修复成本”排序；若候选需要新增课程、路由、依赖或大规模视觉功能，MUST 先缩小为不堆功能的契约修复。

#### Scenario: 多个问题共享同一根因
- **WHEN** 陈旧压力结果、无门槛摘要和能力矩阵误计都来自完成证据缺少尝试与策略绑定
- **THEN** harness 选择“绑定完成证据”作为一个根因切片
- **AND** 把显式填写判断变化等新增交互留为后续候选

#### Scenario: 没有问题满足一天闭环
- **WHEN** 所有候选都不可复现或无法在一天内完成修复与验证
- **THEN** harness 不创建伪精确实施任务
- **AND** 输出阻塞证据和下一步调查计划

### Requirement: 修复与验证闭环
每个被选切片 MUST 依次定义失败基线、规范化预期、失败优先自动测试、最小实现、自动回归、必要手动验证和证据归档。验证 MUST 证明原复现由失败变为通过，并同时证明相邻旧行为未回归；仅构建成功、测试总数增加、页面可点击或人工声称“看起来正常”均 MUST NOT 视为闭环。

#### Scenario: 规范阶段停止在实施前
- **WHEN** 当日任务明确只允许审查和规范阶段
- **THEN** harness 完成 proposal、specs、design、tasks 和严格校验
- **AND** 不修改业务代码、不勾选实施任务、不提交或归档 change

#### Scenario: 后续获批实施
- **WHEN** 用户批准进入 apply 阶段
- **THEN** 实施先加入能复现原问题的失败测试，再完成最小修复
- **AND** 最终报告包含原复现、定向测试、全量回归、手动路径和遗留风险的结果
