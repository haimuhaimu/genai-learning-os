# 提案: 2026-09-11 Daily Adversarial Harness

## 今日轮换角色
零基础学习者

## 单一根因问题
RAG 切块模拟器在「句子切块 + overlap=0」时会产生 token 覆盖空洞（静默数据丢失），导致学习者对切块/重叠与证据覆盖关系形成错误直觉。

## 目标
- 让切块结果在任意合法参数下保持连续覆盖（不引入空洞）。
- 保持既有预算门禁、路由与 storage keys 不变。

## 非目标
- 不新增课程、积分、签到、徽章、排名等机制。
- 不调整性能预算阈值。

## 验证
- 新增失败优先测试复现空洞并在修复后转绿。
- `pnpm ci:check` 全量通过（含 a11y / bundle / route chunk 门禁）。
