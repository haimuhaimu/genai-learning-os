## MODIFIED Requirements

### Requirement: 通用任务状态流
所有带 mission 的 Strategy Case SHALL 复用同一状态流：`待预测 → 预测已锁定 → 策略调整/比较 → 压力测试 → 已形成策略/复盘`；系统 MUST 根据可见且相互一致的行为证据推进状态，不得因仅打开页面、仅调整 control、仅查看资源、仅点击摘要，或沿用与当前 controls 不匹配的压力结果而宣称完成任务。未带 mission 的案例 MUST NOT 被强制进入该状态流。

#### Scenario: 未预测时探索
- **WHEN** 学习者尚未保存预测但调整 controls
- **THEN** 系统可以继续提供即时因果反馈
- **AND** 明确提示先下注才能形成完整任务证据，不伪造锁定预测

#### Scenario: 形成可复盘任务
- **WHEN** 学习者已锁定预测、针对当前 controls 运行至少一次压力测试并点击“形成策略摘要”
- **THEN** 系统进入可生成复盘卡的状态并保存同一次尝试的完成证据包
- **AND** 仅调整 controls、仅点击摘要或使用陈旧压力结果不会被记为“已形成策略”

#### Scenario: 压力测试后再次调参
- **WHEN** 学习者运行压力测试后改变任一 control，使当前 controls 与压力执行时的基础策略不同
- **THEN** 最近压力结果标记为“已过期”，任务阶段回到“策略探索与比较”
- **AND** 过期结果可供复盘历史查看，但不能解锁当前摘要、复盘卡或完成态

### Requirement: 确定性压力测试
系统 SHALL 仅使用当前案例 mission 中声明的 preset 与现有纯计算函数执行压力测试，不使用随机数、网络请求、真实模型或真实服务。结果 MUST 展示总通过/失败、每个门槛的当前值与目标、失败原因，以及 spec 声明的应返回调整的旋钮；记录 MUST 同时保存执行前的基础 controls 和应用 preset 后的有效 controls，重复输入 MUST 产生完全一致的结果。系统 MUST 使用规范化的基础 controls 比较判断结果是否仍适用于当前策略，时间戳或 preset ID 相同不得替代策略一致性。

#### Scenario: 重复运行同一压力 preset
- **WHEN** 学习者以相同当前策略连续运行同一 preset
- **THEN** 两次使用相同 controls 覆盖规则和门槛计算
- **AND** 基础 controls、有效 controls、指标、通过状态、失败原因与旋钮建议完全一致

#### Scenario: 压力测试失败
- **WHEN** preset 计算结果未满足一个或多个门槛
- **THEN** 系统显示“未通过”及每个失败门槛的当前值、比较关系与目标值
- **AND** 为每个失败项提供可聚焦或可定位的 control 名称，而不是直接给出推荐取值

#### Scenario: 压力测试通过
- **WHEN** preset 结果满足全部门槛
- **THEN** 系统显示“通过”及逐项文本证据
- **AND** 通过仅被记录为该基础策略在该 preset 下的可见行为证据，不被表述为真实服务验证或其他策略的验证

#### Scenario: 旧压力记录缺少 controls 绑定
- **WHEN** 系统读取变更前只含 preset、结果、指标和时间的压力记录
- **THEN** 记录继续可读并标记为“历史结果，需重跑后用于当前完成”
- **AND** 系统不得猜测其基础 controls，不得将其视为当前策略的新鲜压力证据

### Requirement: 完成后的可复制复盘卡
系统 SHALL 仅在学习者锁定预测、获得与当前 controls 匹配的压力结果并形成策略摘要后生成复盘卡；复盘卡 MUST 聚合同一次尝试的锁定预测、最终 control 策略、默认基线到最终策略的关键 delta、匹配的最近压力测试结果与一个迁移问题，并 SHALL 支持复制为可读纯文本。系统 MUST NOT 为复制或分享新增后端。

#### Scenario: 摘要形成前
- **WHEN** 学习者尚未点击“形成策略摘要”
- **THEN** 系统不把当前临时 controls 生成或保存为完成态复盘卡
- **AND** 明确说明还缺少的可见步骤

#### Scenario: 缺少预测或新鲜压力结果
- **WHEN** 学习者没有锁定预测、尚未运行压力测试，或最近压力结果与当前 controls 不匹配
- **THEN** “形成策略摘要”不可产生 Mission `level: 2` 完成证据
- **AND** 页面逐项说明缺少“锁定预测”“对当前策略运行压力测试”中的哪些条件，并允许继续探索

#### Scenario: 生成并复制完整复盘卡
- **WHEN** 学习者已锁定预测、针对最终 controls 运行压力测试并形成策略摘要
- **THEN** 复盘卡展示同一次尝试的预测、最终策略、关键 delta、压力结果和迁移问题
- **AND** 点击复制时写入相同语义的纯文本，Clipboard API 不可用时提供可选中文本的降级路径

#### Scenario: 压力结果为失败
- **WHEN** 与最终 controls 匹配的最近压力测试未通过，但学习者仍形成策略摘要
- **THEN** 系统允许形成以“No-Go / 待调整”为结论的完整复盘
- **AND** 完成证据明确保存失败门槛，不把“已完整复盘”表述成“压力已通过”

### Requirement: 能力证据覆盖矩阵
系统 SHALL 在现有首页或进度页展示 4-6 个受控能力维度的轻量矩阵，并仅从结构完整的 Mission 完成证据包和与该证据包绑定的压力通过结果推导证据覆盖。矩阵 MUST 显示证据类型、来源案例和待补证据，不提供综合分、排名、等级、连续签到或徽章，并 MUST 明确声明“证据覆盖不等于能力认证”。变更前缺少尝试或 controls 绑定的旧记录 MUST 保持可读，但 MUST NOT 自动满足新的 Mission 完成或压力验证条件。

#### Scenario: 没有任务证据
- **WHEN** 本机没有结构完整的 Mission 完成证据包
- **THEN** 每个能力维度显示待补证据及可执行的下一步
- **AND** 系统不显示零分、失败等级或能力不足结论

#### Scenario: 形成策略但压力未通过
- **WHEN** 某案例存在结构完整的完成证据包，且其绑定压力结果为失败
- **THEN** 对应维度仅显示“策略形成 / 已完整复盘”证据与来源案例
- **AND** 压力验证证据仍标为待补，不把执行过压力测试等同于通过

#### Scenario: 形成策略且绑定压力通过
- **WHEN** 某案例的完整证据包同时包含与最终 controls 匹配的压力通过结果
- **THEN** 对应能力维度展示策略形成与压力通过两类证据及可返回的案例来源
- **AND** 文案仍保持“证据覆盖”而非认证、分数或保证

#### Scenario: 旧的独立摘要与压力通过记录
- **WHEN** 旧记录分别含有 `level: 2` 摘要和历史通过 preset，但无法证明二者属于同一尝试与同一策略
- **THEN** 系统不得把两条记录拼成新的完整 Mission 证据
- **AND** 页面提供返回案例重跑压力并重新形成摘要的待补动作
